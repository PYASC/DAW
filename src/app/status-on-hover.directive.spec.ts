import { StatusOnHoverDirective } from './status-on-hover.directive';

describe('StatusOnHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new StatusOnHoverDirective();
    expect(directive).toBeTruthy();
  });
});
